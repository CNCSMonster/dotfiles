[user]
    name = cncsmonster
    email = cncsmonster@163.com
[init]
    defaultBranch = main
[core]
    # pager = delta
    editor = code
[interactive]
    diffFilter = delta --color-only
[delta]
    side-by-side = true
    line-numbers-left-format = ""
    line-numbers-right-format = "│ "
    syntax-theme = Nord
[credential]
    # 使用内存缓存（1 小时过期），避免明文存储密码
    # 如需持久化存储，建议使用 git-credential-libsecret（加密存储）
    helper = cache --timeout=3600
[log]
    date = iso-strict-local
[trim]
    bases = develop,master,main
# [http]
# 	postBuffer = 524288000
# 	lowSpeedLimit = 0
# 	lowSpeedTime = 9999
# [url "https://github.moeyy.xyz/"]
# 	insteadOf = git://github.com/
[http]
	postBuffer = 524288000
	lowSpeedLimit = 0
	lowSpeedTime = 9999
